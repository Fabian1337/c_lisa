#include <stdio.h>

int main()
{
    // Typecasting
    char lisa = 'L';
    int l;
    l = (int)lisa;

    printf("L = %i", l);

    int null = 60;
    char nul = (char)null;
    printf("null = %c", nul);
}